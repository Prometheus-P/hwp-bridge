# ============================================
# HwpBridge Environment (Option A)
# - Focus: CLI + MCP (+ WASM build)
# - Web API (hwp-web) env moved to: .env.web.example.disabled
# ============================================

# Default logging
RUST_LOG=info

# Transport: stdio | http
HWP_MCP_TRANSPORT=stdio

# HTTP (only used when HWP_MCP_TRANSPORT=http)
PORT=8081

# Runtime limits
HWP_MAX_FILE_BYTES=26214400
HWP_MAX_DECOMPRESSED_BYTES_PER_SECTION=67108864
HWP_MAX_RECORDS_PER_SECTION=200000
HWP_MAX_CONCURRENT=4

# OPTIONAL (local only)
# Enable dev UI (requires build feature: --features dev-ui)
# HWP_DEV_UI=1
# HWP_DEV_UI_TOKEN=changeme

# OPTIONAL (local only)
# Enable local-path input for tools
# HWP_ALLOW_PATH_INPUT=1
# HWP_PATH_BASEDIR=/absolute/base/dir
